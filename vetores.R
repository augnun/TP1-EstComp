# Vetor aleatório de tamanho 10;
# i.e. 10 realizações de uma U(0,10), arredondadas com a função floor()
set.seed(1986)
vetor10 <- sample.int(10)

# Vetor aleatório de tamanho 100;
# i.e. 100 realizações de uma U(0,100), arredondadas com a função floor()
set.seed(1979)
vetor100 <- sample.int(100)

# Vetor aleatório de tamanho 1000;
# i.e. 1000 realizações de uma U(0,1000), arredondadas com a função floor()
set.seed(42)
vetor1000 <- sample.int(1000)

# Vetor aleatório de tamanho 10000;
# i.e. 10000 realizações de uma U(0,10000), arredondadas com a função floor()
set.seed(51)
vetor10000 <- sample.int(10000)

# Vetor aleatório de tamanho 10000;
# i.e. 10000 realizações de uma U(0,10000), arredondadas com a função floor()
set.seed(2016)
vetor50000 <- sample.int(50000)